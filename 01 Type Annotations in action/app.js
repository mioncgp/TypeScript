// types of values:
// - 1) primitive 7 types: number, boolean, void, undefined, string, symbol, null:
// - 2) object types: functions, arrays, classes, objects
// Type annotaions = Code we add to tell TS what type of value a variable will refer to
// Type inference = TS tries to figure out what type of value a variable refers to
