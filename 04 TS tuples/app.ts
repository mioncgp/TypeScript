// Array-like structure where each element represent some property of a record
const drink: [string, boolean, number] = ["brown", true, 40];
// always consistent order of elements

// another way to defining a tuple
type Drink = [string, boolean, number];

const drinkCoffee: Drink = ["black", false, 0];
